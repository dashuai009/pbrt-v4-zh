# 1.5–1.8 独立复核

结论：四份固定原文及完整本地中英内容已经独立逐段读取、对照，并修复以下遗漏与语义问题。**内容复核完成，保留明确的上游技术问题及公共排版/构建状态；不能标记为无未决问题。**

独占文件为 `chapter-1-Introduction/chapter-1.5-Using_and_Understanding_the_Code.typ`、`chapter-1.6-A_Brief_History_of_Physically_Based_Rendering.typ`、`chapter-1.7-Further_Reading.typ`、`chapter-1.8-Exercises.typ`（后三个同目录）。没有修改公共样式或工作流。

## 独立读取范围

固定提交 `f6d66f0a6e31c3d3ed6a0756c3ad7b4af2dd8c4c` 的 `4ed/Introduction/` 下四个同名 HTML，完整阅读正文与相邻内容；数学以 SVG title 和上下文核对，脚注另外读取原始 tooltip 属性。

- 1.5：51 个本地双语块、12 个子节、6 个目录项、4 步问题报告流程、Allocator 代码片段及三个接口声明、2 条原书脚注、全部行内数学/代码名。重点核对线程安全的条件、GPU/CPU动态分派、内存分配和调试叙述。
- 1.6：31 个双语正文/列表块、Research/Production 两部分、3 项制作优势、两幅电影图及全部署名/版权图注；逐项核对历史人物、年份、计算量、价格、256×256及7小时等数据。直接引用固定子模块的 `gravity.png`、`alita.png`，没有替换为其他图像。
- 1.7：13 段全部对照，含初校补译的后5段；完整阅读并核对61条References，不只核对数目。另以逐条去空白文本比较确认61条与固定原文字符内容相同，作者、次序、年份、题名、刊物、页码均未遗漏。
- 1.8：**固定原书实际只有1道习题，不是8道**。原书正文标题为单数 `Exercise`；已将本地英文标题同步为单数。完整对照这一道习题及难度①，不新增题目。

本复核没有运行旧 Scripts 或批量模型脚本。少量临时结构化读取仅用于源文提取、逐条文本比较和引用校验；没有生成批量翻译。

## 发现及修复

1. 1.5 线程安全段括号内漏掉“计算密集的方法实现”这一限定。补成：计算量大的方法实现可以在内部用 ParallelFor 并行，但这与调用者是否可并发调用是两回事。
2. 1.5 脚注 image maps 从“图像映射”修为“图像贴图”；修正单元测试段把 alternatively 译为“相反”的逻辑关系，并改善调试段语病。
3. 1.5 断言段与固定原书 B.3.6 的 CHECK(x) 实现存在直接冲突。保留英文原文，在中文中明确指出原段的“为真失败”与宏的“实参不真失败”不一致，并引导以宏定义为准，没有静默反转原文。
4. 1.5 “为每线程在栈上分配实例”与固定原书 ThreadLocal 实现也不一致。附录 B.6 的 `ThreadLocal` 使用 `std::vector<pstd::optional<Entry>> hashTable` 保存对象；新增译注明确最终存储不能一概称为线程栈。英文原文保留。
5. 1.6 两电影图已由初校替换正确，本复核确认文件与原书图像一致；修正 Gravity 图注单幅图像被说成多幅、公司名称空格，Alita 的完整版权译为“© 2018 Twentieth Century Fox Film Corporation。保留所有权利。”
6. 1.6 保留 radiosity 历史段“至少在相对简单场景上”的限定语气，修正若干“追求…导致…”的翻译腔，统一体积渲染方程。
7. **1.7 正文全部文献引用仍为无链接作者年份。** 根据固定原文13段中的40处citation目标，恢复两语共80个本地书目链接。尤其逐个区分同年多个作者：1994、1996、2017，以及2018五篇系统论文，不能仅按年份映射。
8. 1.7 的61条新增书目文字完整，但原来全是单一 text，丢失原文题名/刊名强调和两条外链。恢复这些结构，不改文献题名或其内容；复核后再次逐条比较，去空白文本完全一致。恢复正文英文书名、系统名的原文强调。
9. 1.8 整道题重新润顺中文，保留调试符号、断点位置、单线程建议的条件，以及随阅读进展重复追踪的要求。没有改 `-nthreads 1` 的实质参数文本。

## 仍需明确保留的上游问题

- **CODE-UPSTREAM-01：allocate_object 默认构造。** 固定1.5原文声称其调用默认构造函数，但本节未提供pstd对应实现。此项仍待对指定实现进一步核实，不能根据通用标准库印象擅自改译或标作已经证实。英中忠实保留，问题不隐藏。
- **CODE-UPSTREAM-02：断言真假。** 已核实矛盾依据为固定 `4ed/Utilities/User_Interaction.html#CHECK` 和 `#sec:assertions`：代码 `CHECK(x) (!(!(x) && (LOG_FATAL(...), true)))`，文字同样说 specified condition is not true。中文已附明确译注。
- **CODE-UPSTREAM-03：线程栈存储。** 已核实依据为固定 `4ed/Utilities/Parallelism.html#ThreadLocal`，对象在hashTable的vector存储中；本段与1.3.4的实际组织关系已加译注。
- **CODE-UPSTREAM-04：习题命令行横线。** 固定源文用排印en dash `–nthreads 1`；本地沿用单ASCII横线 `-nthreads 1`。固定网站正文没有给出ParseArg实现或双横线nthreads用法，未猜测改变选项语法；这只代表保留转录形式，不宣称该命令已运行验证。

## 交叉纠错

复核完整1.7书目时发现，固定原书也缺少1.2图1.4引用的 `cite:DisneyMoana`。经主 agent 明确重新授权，修复了本 agent 先前在1.2恢复的失效外链：保留作者年份，中文声明源文缺失该书目。详见 `review-ray-tracing.md` 的 RAY-UPSTREAM-05 补记。1.2已再次释放，通知主 agent刷新指纹。

## 验证与布局边界

- 26个原书页面/锚点外链对固定子模块验证：无缺页、无缺锚点。
- 1.5–1.8的61个本地书目引用目标均存在；1.7正文80个恢复链接对应原文40个目标，已逐段校验。
- 61条书目在恢复强调/链接后，逐条去空白文本与原文完全一致。
- `git diff --check` 通过。
- Typst固定0.13.1，正式全书入口 `main.typ`，`--root . --font-path fonts`。上一轮中文/英文/双语完整构建均退出0。最后两条上游译注及1.2纠错后的重建遇到主 agent正在集成的公共 `styles/common.typ:41` 语法错误（expected colon），三语相同；未绕过或修改公共模板。后续结果补记于下。
- 内容复核与布局验收分开：公共样式仍在修改，本报告不能替代四节全部页面、全书PDF及网站的最终视觉/链接验收。

最终验证补记：公共语法修复后，最终状态三语正式全书构建全部退出0，输出 `/tmp/review-code-history-zh.pdf`、`/tmp/review-code-history-en.pdf`、`/tmp/review-code-history-bi.pdf`。实际查看最终中文第79页（断言译注）、85/86页（两电影图及完整图注）、88页（补译后段与书目开始）、91页（书目尾部）、92页（唯一习题）。这些代表页无裁切、重叠或乱码，文献强调和新增译注可见；未将该检查扩大宣称为全部页面或网页布局通过。

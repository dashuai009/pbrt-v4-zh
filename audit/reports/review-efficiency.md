# 2.2 独立复核

状态：正文已独立复核并修复；两项已确认的固定上游数值矛盾仍须保留在问题清单。不得把本报告当作全书完成。

复核者 edit_introduction，非初校 agent。独占 chapter-2-Monte_Carlo_Integration/chapter-2.2-Improving_Efficiency.typ。

## 重新阅读

完整阅读固定上游 4ed/Monte_Carlo_Integration/Improving_Efficiency.html（f6d66f0a6e31c3d3ed6a0756c3ad7b4af2dd8c4c）及本地中英两栏。HTML 辅助抽取保留每个 MathJax SVG title，三条脚注另读原始 tooltip，不依赖初校摘要。前序任务已完整读 2.1 作为上下文；本次定位 8.5 的既有标签确认引用。

覆盖 63 个双语正文块、完整 26 个显示公式（原书 2.12–2.18 为其中编号公式）、全部行内公式、2 组图/5 个图片资源及图注与 a/b/c 标识、2 个完整 C++ 函数代码块、2 个片段标题与跳转、3 条脚注、所有小节标题。无独立习题、延伸阅读或数据表。

覆盖原书分层采样、重要性采样、MIS、MIS Compensation、俄罗斯轮盘赌、分裂六部分从头至尾，不是只检查修改差异。

## 复核确认

- 逐式核对分层方差推导、n_i=v_i n 条件、权重求和条件、PDF 为零时对应权重为零、单样本 q_i 因子、MIS 补偿“一个或多个但非全部”、轮盘赌 F-qc 分子及 xi>q 分支、分裂 nm 采样公式。未擅自更改数学假设。
- ~ 在实际 PDF 中正确呈现为分布关系的波浪号；未误当等号。
- 初校报告的上游矛盾确实存在：高斯公式中心 1/2，但正文写 1/4 及 0.2/0.3；差 PDF 中段 0.2，正文却写 0.4。原文均保留，中文译注明确提示，不猜数值。
- BalanceHeuristic 和 PowerHeuristic 全部代码与上游逐行匹配，包含 nf/ng、fPdf/gPdf 和 Sqr 调用。
- 分段区间的 Typst `\,` 在实际渲染中为逗号，未将其误改成薄空白；半开端点正确。

## 独立发现并修复

- w_i(X) 改为 w_i (X)，显式隔开下标与函数参数，避免 Typst 把括号吸入下标。实际显示参数仍在基线上。
- cases 的值和条件原先贴成“0.1x”，在条件列增加 quad；PDF 三行、坏 PDF 三行、轮盘赌分支均实际复看。
- 两个代码片段标题原先用裸方括号，PDF 印出了额外的方括号，而且 PowerHeuristic 标题留在上一页。改为 sticky block，去除多余括号，并与后续代码保持同页。
- 两条译注移至对应段落末尾，避免把“1/4 附近”拆成跨框句子。
- MIS 条件的“条件：，”重复标点修复；“层中被积函数的真实值”明确为公式所定义的真实均值。
- 补回本地文献文字遗漏的 Veach、Owen 作者名；源文不是只有 1997／1998 年份。
- 源文 8.5、图注 2.6 原硬编码引用接到本地实际标签；保留 i-figured 的 eqt:/fig: 形式。

## 实际构建与视觉检查

使用 `/tmp/pbrt-tools/typst-aarch64-apple-darwin/typst`，版本 0.13.1 (8ace67d9)，正式 main.typ、原公共 pbrt 模板、--font-path fonts：
- LANG_OUT=zh 全书通过，输出 /tmp/review-efficiency-full-zh.pdf。
- LANG_OUT=en 全书通过，输出 /tmp/review-efficiency-full-en.pdf。
- 不设 LANG_OUT 的对照版全书通过，输出 /tmp/review-efficiency-full-bilingual.pdf。

使用 PDF 技能的实际渲染流程，以 pdftoppm 将中文全书 PDF 96–104 页全部转为图片逐页查看，覆盖本节全部图、公式、代码、脚注。布局修复后再次查看 99、101、102、103 页：条件列分离、代码标题与代码同页，MIS 参数/下标和~关系正常，图轴和样本点清晰，均未见裁切。公共模板并行更新后中文图注已显示“图/公式”。

英语/对照版本轮只验证全书构建，未逐页视觉验收；主 agent 仍需统一进行三语言最终全书验收。Veach/Owen 作者补回是最后一个文字修正；不据此扩大图片检查范围。

本报告保留源文本原有数学推导的适用前提，不将“分层永不增方差”“轮盘赌总增方差”等话语扩展到未满足其条件的情形。git diff --check 通过。

### 最新并行集成快照补充

最后补回 Veach/Owen 作者后，重新运行中文全书时，其他 agent 正在修改的 1.5 出现 3 个 `tbl:plug-in-types` 缺失目标（53／299／325 行），故最新全书检查失败；此前布局修复后的三语成功记录仅代表当时快照。已通知主 agent。没有修改其他 agent 独占文件，也未用旧 PDF 冒充最后快照成功。

最后英语、对照版全书重编均退出 0；中文再试仍在 1.5 的同一标签失败，说明这一集成问题具有语言模式差异。2.2 文件现已释放。

# 1.3–1.4 精校交接

状态：`awaiting_review`。尚须另一 agent 重读固定原文独立复核，不能标为已精校。

独占文件：
- `chapter-1-Introduction/chapter-1.3-pbrt_System_Overview.typ`
- `chapter-1-Introduction/chapter-1.4-How_to_Proceed_through_This_Book.typ`

固定上游：`f6d66f0a6e31c3d3ed6a0756c3ad7b4af2dd8c4c`。
对应文件：`4ed/Introduction/pbrt_System_Overview.html`、`4ed/Introduction/How_to_Proceed_through_This_Book.html`。相邻 1.2 的光传输背景此前已由本 agent 完整对照；1.4 后文承接代码使用指南。未运行仓库旧 Scripts，未调用模型批处理。辅助工具仅用于读 HTML、枚举代码/链接和比较图像；中文由 agent 逐段编写。

## 实际范围

- 1.3 全部正文：系统接口与14类基本类型；执行三阶段；main()；Integrator；ImageTileIntegrator；RayIntegrator；RandomWalkIntegrator，80 个双语正文块（部分对应上游两个连续段落，逐一对照其完整文字）。
- 1.3 表 1.1 的 14 行、全部三个字段、表头、图注、2 条脚注、8 个图注（图 1.13–1.20）、9 个图像资源、公式 (1.2) 及其全部行内数学。
- 1.3 对照所有 53 个非嵌套正文代码片段，恢复名称与对应固定原文锚点。部分本地类定义原已展开上游折叠代码；其现有代码内容保留并逐项对照，不以重复展开计作新的原书片段。拆开被合在一个代码块中的三组片段。
- 1.4 全部 13 个双语块、3 个难度项、所有标题、章节引用、原书图片入口和在线版说明。没有独立数学、代码、图像或新表格。

## 主要修复

### 完整性与英文转录

1. 补回 `CleanupPBRT();` 的独立正文片段（`fragment-Cleanupafterrenderingthescene-0`）。
2. 补回末尾 BSDF 段整条丢失脚注：仅反射 BSDF 可只在与射线同侧采样，但简单积分器不作此优化。英中同时补齐。
3. 补回英文 `<<Optionally write current image to disk>>` 与 `<<Issue warning if unexpected radiance value is returned>>` 两处漏掉的片段名；后者原只剩空尖括号。
4. 恢复 53 个正文片段的独立标题并指向原书精确锚点；片段名不再混在 C++ 行号中。保留片段占位与既有展开实现，不把 HTML 折叠展开的重复代码误记为额外算法。
5. `RandomWalkIntegrator` 展开代码中的 `\pi` 是转录损坏，固定原书为 C++ 常量 `Pi`，已修复。未更改算法、方法签名、常量值或代码标识符。
6. 图 1.20 左图原误用图 1.19 的 `random-walk-insanity.png`，改用 `watercolor-randomwalk.png`，与上游 JERI 配置 (a) watercolor-randomwalk.exr、(b) watercolor-path.exr 一致；补 a/b 标记，保持顺序。
7. 表 1.1 原英中各一个 figure，在双语 PDF 产生重复编号并影响后续表。合并为一个表，以 ez_caption 切换表头与说明，统一 `<plug-in-types>` 和 `@tbl:plug-in-types`。1.5 外部引用已通知主 agent 协调修改。
8. 数学转录：明确 `L_(e)(…)`、`L_(i)(…)`，避免函数参数落入下标；球面积分域恢复 `cal(S)^2`；`omega prime`/`theta prime` 恢复真正上标撇号；概率密度分母写明 `1 / (4 pi)`，依据固定原文公式及其后一段的 4π 缩放解释。不改等式或近似关系。
9. 删除标题中硬编码 1.3.5/1.3.6 与皇冠图注硬编码“图1.16”；全部标题提供中英两语。
10. 修复 HTML 转录留下的链接后多余分号、无效 `"<Integrator>"` 字符串链接及相对 HTML 链接。1.4 英文恢复第8/11/14章引用，删除串行列表破损，恢复三个独立难度项。

### 中文

完整重读并修正各段，重点包括：
- primitive→图元，radiance→辐亮度，integrator→积分器；ray differential→射线微分，tile→图块，tag-based dispatch→基于标签的分派（已报主 agent）。
- 单个 Primitive 与光源数组的修饰关系；预处理时整个场景包围盒尚不可用的条件；首次调用与只在非空时初始化 VisibleSurface 的条件；线程局部采样器和临时内存的生命周期。
- 补回 ParseFiles 段 C.2/C.3 的类介绍位置；恢复不同处理器处理不同图块的漏译；正确表述分批样本数 1、1、2、4…及上限，保留并行领取任务和尾部闲置的权衡。
- 光谱辐亮度、发射/入射/出射、加权像素更新、无参与介质假设、时间可逆性、递归终止与独立估计平均的条件。
- 修复“没有交叉光源的概率”等反义/病句，明确点光源相交概率为零；保留理想镜面方向无法随机采到的限制。
- 全图注精校；皇冠保留发射光谱来自实测这一限定；54.5 倍误差降低明确为降至原来的 1/54.5，避免“减少若干倍”的歧义。
- 保留原书撰写时态与日期，1.4 在线版段不擅自改写为最新事实。

## 原文与本地仍需区分的事项

- **SYS-STRUCT-01**：原文描述纸书页边 mini-index，但本地仍未实现页边小索引。英中忠实保留该描述；这项本地功能缺失已交主 agent，不宣称索引完成。
- **SYS-SOURCE-01**：固定上游 1.3.3 文字指向 `cpu/integrator.h/.cpp`（单数），表1.1则为 `cpu/integrators.h/.cpp`（复数）。本次保留固定上游文字和原文来源链接，不凭当前远程仓库猜改上游。
- **SYS-SOURCE-02**：表1.1 FloatTexture 行源文件和章节单元格为空，固定上游同样为空；保留空格，不猜填。
- **SYS-EXTRA-01**：固定 HTML 还含嵌套折叠代码展开，例如正文明确不展示的命令行解析、警告处理、部分未在本节单独叙述的构造器/辅助方法。这些与 53 个正文片段分开记账。本地保留原有展开类代码，但没有新增完整的 HTML 折叠交互树；所有53个正文片段标题均可跳回上游查看展开。不能据本报告宣称全部在线交互代码展开已在本地复刻。
- **SYS-IMAGE-01**：3 张本地 PNG 为 SDR 图；原书源为 EXR。使用 sips 从固定 EXR 临时解码后，确认尺寸、视角、内容和噪声对应正确；与本地 PNG 的每通道平均8位差异为0.23–2.70，不宣称色彩或 HDR 交互严格等同。全书在线站的 HDR 展示仍需单列能力状态。

## 检查

- 108 个原书页面/锚点链接逐一在固定子模块核实：0 缺页、0 缺锚点。GitHub 外链未据此宣称远程可达。
- 三种完整 PDF 使用 Typst 0.13.1，`--root . --font-path fonts`，中文 `--input LANG_OUT=zh`、英文 `--input LANG_OUT=en`、双语不设置语言输入。输出临时文件 `/tmp/system-overview-{zh,en,bi}.pdf`。
- 首轮三语均退出0。视觉读中文47/69/71/73页、双语65页，核对单表、式1.2、正确32spp双图和1.4文本；表1.1在三语均只出现一次且编号1.1。检查中发现中英第三列表头换行不佳，已调整列宽为22/58/20，最终构建结果将在交接时补记。
- 观察到公共 ez_caption 在双语标题/图注中仍无分隔，已通知主 agent。公共 table caption 的图/表错误已由主 agent 后续修复，最新双语页显示“表1.1”。没有改公共模板。
- `git diff --check` 通过。

下一步：独立复核应重读以上固定HTML、完整两语正文、53代码片段和所有图表，重点重新检查数学转录、丢失脚注、表计数与图1.20源图，并明确保留上述结构/上游限制。

最终补记：正文最终措辞后三语构建均退出0；仅最后列宽调整后额外重建双语并再次查看第65页。第三列表头已能完整显示，第一列双语表头仍折成两行（未裁切），公共双语分隔仍待处理；不把该页记为最终排版完美。正文已释放给独立复核。

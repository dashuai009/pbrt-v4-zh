// Checks real exported figure bodies. A caption alone is not an image.
import fs from 'node:fs';import path from 'node:path';import {parseFragment} from 'parse5';
const root=process.argv[2]||'output/site';const issues=[];let figures=0,math=0;
const walk=function*(n){yield n;for(const c of n.childNodes||[])yield*walk(c)};
const attr=(n,k)=>n.attrs?.find(a=>a.name===k)?.value;
const visible=n=>n.tagName==='figcaption'?false:n.nodeName==='#text'?!!n.value.trim():['img','svg','table','pre','code'].includes(n.tagName)||(n.childNodes||[]).some(visible);
function check(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const f=path.join(d,e.name);if(e.isDirectory()){check(f);continue}if(!f.endsWith('.light.html'))continue;const doc=parseFragment(fs.readFileSync(f,'utf8'));for(const n of walk(doc)){if(n.tagName==='svg' && /^0\s+0\s+0\s+0$/.test(attr(n,'viewBox')||''))issues.push({file:f,reason:'SVG has zero-sized viewBox'});if(n.tagName==='image' && (Number(attr(n,'width'))===0 || Number(attr(n,'height'))===0))issues.push({file:f,reason:'Image has zero rendered dimensions'});if(n.tagName==='figure'){figures++;if(!(n.childNodes||[]).some(visible))issues.push({file:f,reason:'Figure has no rendered body'});}if(n.attrs?.some(a=>a.name==='class'&&a.value==='inline-math')){math++;if(![...walk(n)].some(c=>c.tagName==='svg'))issues.push({file:f,reason:'Inline math has no SVG'});}}}}
check(root);fs.writeFileSync('output/rendered-content-report.json',JSON.stringify({figures,math,issues},null,2));console.log(JSON.stringify({figures,math,errors:issues.length,sample:issues.slice(0,6)}));if(issues.length)process.exit(1);
